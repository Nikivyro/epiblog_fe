import React from 'react'

export default function EditUser({userId}) {
    
    
    /// la rotta per fare la patch users/:userId/update
    return (
    <div>
        <h3>EditUser: {userId}</h3>
        Form per editare il profilo
    </div>
  )
}
